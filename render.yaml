services:
  - type: web
    name: pen-landing
    env: node
    plan: starter
    buildCommand: "npm install && npm run build && npx prisma generate"
    # `npm run start` already runs `prisma migrate deploy`. Never add `prisma db
    # seed` here — it would re-seed on every boot, not just first deploy.
    startCommand: "npm run start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: pen-landing-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: pen-landing-redis
          type: redis
          property: connectionString
      - key: NEXT_PUBLIC_SITE_URL
        value: ""
        sync: false
      - key: AUTH_JWT_SECRET
        generateValue: true
      - key: AUTH_REFRESH_SECRET
        generateValue: true
      - key: AUTH_ADMIN_EMAIL
        value: "admin@example.com"
        sync: false
      # No default: a placeholder password that boots is worse than a failed
      # boot. Set this explicitly in the dashboard (min 8 chars).
      - key: AUTH_ADMIN_PASSWORD
        sync: false
      - key: AUTH_ALLOWLIST_ORIGINS
        value: ""
        sync: false
      - key: STRIPE_SECRET_KEY
        value: ""
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        value: ""
        sync: false
      - key: TWILIO_SID
        value: ""
        sync: false
      - key: TWILIO_AUTH
        value: ""
        sync: false
      - key: TWILIO_PHONE
        value: ""
        sync: false
      - key: SENTRY_DSN
        value: ""
        sync: false
      - key: NEXT_PUBLIC_SENTRY_DSN
        value: ""
        sync: false

databases:
  - name: pen-landing-db
    plan: starter
    # Render managed PostgreSQL: automatic daily backups on paid plans.
    # Free/starter tier: run scripts/backup.sh manually or schedule via cron.
    # Restore: psql $DATABASE_URL < backup.sql (after gunzip)

# Redis: distributed rate limiting across instances
# Safe to remove if running single-instance (in-memory fallback is automatic)
  - type: redis
    name: pen-landing-redis
    plan: starter
    maxmemoryPolicy: noeviction
